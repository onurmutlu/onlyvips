# OnlyVips Backend API - HashiCorp Vault Yapılandırması Örneği

# Secret backend olarak HashiCorp Vault'u kullan
SECRET_PROVIDER=vault

# Vault Bağlantı Ayarları 
VAULT_URL=https://vault.example.com:8200
VAULT_TOKEN=your-vault-token
VAULT_MOUNT=secret
VAULT_PATH=onlyvips

# Servis Yapılandırması (Sadece Fallback Değerler)
PORT=8000
NODE_ENV=production
LOG_LEVEL=info

# Veritabanı fallback
DATABASE_URL=mongodb://localhost:27017/onlyvips 